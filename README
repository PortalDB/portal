Set $SPARK_HOME environment variable 

To compile:
    sbt package

To run Snapshot Graph Project:
    sbt "run-main edu.drexel.cs.dbgroup.graphxt.SnapshotGraphTest {params}"
    	** where {params} could be for example: "G4_U --agg 5 universal --pagerank --data ../../resources --type SG --partition EdgePartition2D 8"

To run MultiGraph Project:
    sbt "run-main edu.drexel.cs.dbgroup.graphxt.MultiGraphPTest {params}"
    	** where {params} could be for example: " G2_E --agg 5 existential --count --data ../../resources --type MG --partition HybridRandomTemporal 32"
